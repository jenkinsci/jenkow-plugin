txt=\
  This Git repository exposes the content of the $JENKINS_HOME/jenkow directory via Git. \
  Anyone can pull/clone this repository, but only the administrators can push.

jeus=\
  This local Eclipse update site can be used in cases where the \
  <a href="{0}eclipse.site">global update site</a> does not work (because it is a composite update site).
